function makeFunc(){
    const msg="Closure call";
    function displayMsg(){
        console.log("msg",msg)
    }
    return displayMsg;
}
const myfunc=makeFunc();
myfunc();

//output:  msg Closure call
